```
  /core             -> Core libraries (router wrapper, middleware handler, etc.)
  /modules          -> Plug-and-play modules (e.g., Auth, User, Logger, DB)
  /examples         -> Sample usage with main.go
  /plugins          -> Optional middlewares or tools (e.g., Swagger, Tracing)
  /config           -> Centralized configuration

```